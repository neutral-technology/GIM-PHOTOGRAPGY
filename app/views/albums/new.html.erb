<div class="flex justify-center items-center min-h-screen">
    <div class="panel sm:w-[480px] m6 max-w-lg w-full" x-data="{ submitting: false }">
      <h2 class="font-bold text-2xl mb-3 text-black">Nouvelle album</h2>
      <p class="mb-7 text-black">Entrez les details de l'album</p>
        <%= render 'form', album: @album, clients: @clients %>
    </div>
</div>