<div class="page" data-page-id="<%= page.id %>">
  <div class="blocks sortable" data-controller="sortable editor">
    <% page.blocks.order(:position).each do |block| %>
      <%= render "/brochures/editor/block", block: block %>
    <% end %>
  </div>
</div>
