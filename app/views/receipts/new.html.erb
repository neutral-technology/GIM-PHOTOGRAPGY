<div class="flex justify-center items-center min-h-screen">
    <div class="panel sm:w-[480px] m6 max-w-lg w-full" x-data="{ submitting: false }">
      <h2 class="font-bold text-2xl mb-3 text-black">Ajouter une recete</h2>
        <%= render "form", receipt: @receipt %>
    </div>
</div>